#include <stdio.h>

typedef struct{//Define a struct, sendo assim, um tipo de dado nomeado
    int inteiros;//Declara como normalmente se faz, declara o tipo de dado, e o nome que vc deu para a variavel
    char string[10];//Declara o tipo char, em seguida, declara quantos caracteres serão permitidos nesse endereço de memória
}   atividade3;//Nomeia a sua struct